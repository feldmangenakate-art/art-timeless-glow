// TodayInHistory section is now integrated directly into the Hero component.
// This component is kept for backward compatibility but renders nothing.
export default function TodayInHistory() {
  return null;
}
